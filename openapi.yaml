# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Analytics API
    version: 0.0.1
paths:
    v1/api/analytics/event:
        post:
            tags:
                - Analytics
            operationId: Analytics_EventAnalytics
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/EventAnalyticsRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/EventAnalyticReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        EventAnalyticReply:
            type: object
            properties:
                xAxis:
                    type: array
                    items:
                        type: string
                yAxis:
                    type: array
                    items:
                        type: string
        EventAnalyticsRequest:
            type: object
            properties:
                events:
                    type: array
                    items:
                        $ref: '#/components/schemas/EventAnalyticsRequest_Event'
                eventsView:
                    $ref: '#/components/schemas/EventAnalyticsRequest_EventsView'
                origin:
                    type: string
        EventAnalyticsRequest_Event:
            type: object
            properties:
                analysis:
                    type: string
                analysisDesc:
                    type: string
                eventId:
                    type: string
                eventName:
                    type: string
                eventNameDisplay:
                    type: string
                eventType:
                    type: string
                relation:
                    type: integer
                    format: int32
                type:
                    type: integer
                    format: int32
                filters:
                    type: array
                    items:
                        $ref: '#/components/schemas/EventAnalyticsRequest_Filter'
        EventAnalyticsRequest_EventsView:
            type: object
            properties:
                groupBy:
                    type: array
                    items:
                        type: string
                timeParticleSize:
                    type: string
                tableSorts:
                    type: array
                    items:
                        type: string
                byType:
                    type: string
                filters:
                    type: array
                    items:
                        $ref: '#/components/schemas/EventAnalyticsRequest_Filter'
                recentDay:
                    type: string
        EventAnalyticsRequest_Filter:
            type: object
            properties: {}
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
tags:
    - name: Analytics
